# GPT Usage

Use GPT-assisted utilities for bounded tasks such as summarization, selector recovery, or synthetic data generation.

Keep prompts explicit, validate outputs, and avoid turning the model into a hidden source of truth for assertions.
