# Playwright Basics

Use Playwright features intentionally:

- Fixtures for reusable setup.
- Projects for browser or environment variants.
- Traces, screenshots, and videos only when they add debugging value.
- `expect` assertions over manual polling where possible.
