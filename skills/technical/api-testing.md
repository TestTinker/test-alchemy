# API Testing

API abstractions live under `api/`:

- `clients/` for HTTP client setup.
- `endpoints/` for service-specific operations.
- `models/` for request and response types.

Use API tests for fast validation of business rules, permissions, and contracts.
