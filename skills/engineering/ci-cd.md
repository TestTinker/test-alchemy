# CI/CD

CI should install dependencies, run linting or type checks, execute the Playwright suite, and publish reports.

Use separate workflows or matrix jobs when browser, environment, or suite partitions need independent scaling.
