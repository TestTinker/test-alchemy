# Test Data

Store reusable data by lifecycle:

- `static/` for committed fixtures.
- `dynamic/` for runtime-generated artifacts.
- `schemas/` for validation contracts.

Test data should be versioned, traceable, and isolated per scenario when parallel execution matters.
