# Environment

Environment configuration belongs in `config/env`.

Each environment exports URLs, credentials sources, and execution flags. Sensitive values should come from environment variables or secret stores, not committed plaintext.
