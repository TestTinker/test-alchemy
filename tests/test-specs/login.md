# Login Scenario

Goal: validate that a valid user can authenticate and reach the dashboard.

Preconditions:
- User account exists.
- Application is reachable.

Assertions:
- Login form accepts credentials.
- User lands on dashboard.
- Session is established.
